import { createReadStream, existsSync } from 'node:fs';
import { mkdir, writeFile } from 'node:fs/promises';
import { createServer } from 'node:http';
import path from 'node:path';
import { chromium } from 'playwright';

const root=process.cwd(),dist=path.join(root,'dist'),reportDir=path.join(root,'reports/public/page-recovery/commercial-retail-shop-renovation-kl');await mkdir(reportDir,{recursive:true});
const mime={'.html':'text/html; charset=utf-8','.css':'text/css','.js':'text/javascript','.png':'image/png','.jpg':'image/jpeg','.jpeg':'image/jpeg','.webp':'image/webp','.svg':'image/svg+xml'};
const server=createServer((request,response)=>{const url=new URL(request.url,'http://127.0.0.1');const pathname=decodeURIComponent(url.pathname).replace(/^\/rkreno(?=\/|$)/,'')||'/';let file=path.join(dist,pathname.replace(/^\//,''));if(pathname.endsWith('/'))file=path.join(file,'index.html');if(!existsSync(file))return response.writeHead(404).end('Not found');response.setHeader('content-type',mime[path.extname(file).toLowerCase()]||'application/octet-stream');createReadStream(file).pipe(response);});
await new Promise((resolve)=>server.listen(0,'127.0.0.1',resolve));const port=server.address().port;const browser=await chromium.launch({headless:true});const route='/commercial-retail-shop-renovation-in-kuala-lumpur/';
const viewports=[{name:'desktop',width:1440,height:1000},{name:'tablet',width:768,height:1024},{name:'mobile',width:390,height:844}],results=[],smokeResults=[];
try{
  for(const viewport of viewports){const page=await browser.newPage({viewport});const consoleErrors=[],failedRequests=[];page.on('console',(message)=>{if(message.type()==='error')consoleErrors.push(message.text());});page.on('requestfailed',(request)=>failedRequests.push(request.url()));await page.goto(`http://127.0.0.1:${port}/rkreno${route}`,{waitUntil:'networkidle'});await page.evaluate(()=>{document.querySelectorAll('.retail-page img').forEach((image)=>{image.loading='eager';});window.scrollTo(0,document.documentElement.scrollHeight);});await page.waitForFunction(()=>[...document.querySelectorAll('.retail-page img')].every((image)=>image.complete&&image.naturalWidth>0),null,{timeout:10000});await page.evaluate(()=>window.scrollTo(0,0));await page.waitForTimeout(150);await page.evaluate(()=>document.querySelector('.rk-header')?.classList.remove('is-stuck'));await page.addStyleTag({content:'.rk-header.is-stuck .rk-header__desktop,.rk-header__desktop,.rk-header.is-stuck .rk-mobilebar,.rk-mobilebar{position:static!important;inset:auto!important}'});
    const metrics=await page.evaluate(()=>({overflow:document.documentElement.scrollWidth-document.documentElement.clientWidth,brokenImages:[...document.querySelectorAll('.retail-page img')].filter((image)=>image.complete&&image.naturalWidth===0).length,h1Count:document.querySelectorAll('.retail-page h1').length,imageCount:document.querySelectorAll('.retail-page img').length,tocCount:document.querySelectorAll('.retail-toc a').length,challengeCount:document.querySelectorAll('.retail-challenge-grid article').length,serviceCount:document.querySelectorAll('.retail-services-grid article').length,priceRowCount:document.querySelectorAll('.retail-price-table tbody tr').length,workflowCount:document.querySelectorAll('.retail-workflow li').length,whatsappCount:document.querySelectorAll('.retail-page a[href^="https://wa.me/"]').length,minBodyFont:Math.min(...[...document.querySelectorAll('.retail-page p:not(.retail-kicker),.retail-page li,.retail-page td')].filter((node)=>node.getBoundingClientRect().height>0).map((node)=>parseFloat(getComputedStyle(node).fontSize)))}));
    await page.addStyleTag({content:'.skip-link:not(:focus){position:fixed!important;top:-1000px!important}'});
    if(viewport.name==='mobile'){const menuButton=page.getByRole('button',{name:/open navigation/i}).first();await menuButton.click();metrics.mobileMenuOpened=await page.locator('.rk-header.is-drawer-open').count()===1;await page.keyboard.press('Escape');}
    await page.screenshot({path:path.join(reportDir,`${viewport.name}.png`),fullPage:true});results.push({...viewport,consoleErrors,failedRequests,...metrics});await page.close();
  }
  const smokePage=await browser.newPage({viewport:{width:390,height:844}});const routes=['/','/blog/','/aircond-installation-kl/','/upah-pasang-aircond-selangor/','/upah-pasang-aircond-selangor-panduan-harga-pemasangan-2026/','/house-renovation-in-kuala-lumpur-the-ultimate-planning-cost-guide-2026/','/pakej-deep-cleaning-rumah-kl-termasuk-pre-hari-raya/','/house-renovation-in-kuala-lumpur/','/electrical-services-selangor/','/plaster-ceiling-contractor-kl/','/contact-us/'];
  for(const pathname of routes){const response=await smokePage.goto(`http://127.0.0.1:${port}/rkreno${pathname}`,{waitUntil:'networkidle'});smokeResults.push({pathname,status:response?.status()??0,h1Count:await smokePage.locator('h1').count(),headerCount:await smokePage.locator('.rk-header').count(),footerCount:await smokePage.locator('[data-shared-footer]').count(),retailGuideCount:await smokePage.locator('[data-commercial-retail-shop-renovation-kl]').count()});}await smokePage.close();
}finally{await browser.close();await new Promise((resolve)=>server.close(resolve));}
const failures=results.flatMap((item)=>[item.overflow>1?`${item.name}: overflow ${item.overflow}`:null,item.brokenImages?`${item.name}: broken images ${item.brokenImages}`:null,item.consoleErrors.length?`${item.name}: console errors`:null,item.failedRequests.length?`${item.name}: failed requests`:null,item.minBodyFont<15?`${item.name}: body text ${item.minBodyFont}px`:null,item.h1Count!==1?`${item.name}: H1 count ${item.h1Count}`:null,item.imageCount!==1?`${item.name}: image count ${item.imageCount}`:null,item.tocCount!==5?`${item.name}: TOC ${item.tocCount}`:null,item.challengeCount!==4?`${item.name}: challenges ${item.challengeCount}`:null,item.serviceCount!==4?`${item.name}: services ${item.serviceCount}`:null,item.priceRowCount!==4?`${item.name}: price rows ${item.priceRowCount}`:null,item.workflowCount!==4?`${item.name}: workflow ${item.workflowCount}`:null,item.whatsappCount!==1?`${item.name}: WhatsApp ${item.whatsappCount}`:null].filter(Boolean));if(!results[2].mobileMenuOpened)failures.push('mobile: navigation did not open');for(const smoke of smokeResults){if(smoke.status!==200||smoke.h1Count!==1||smoke.headerCount!==1||smoke.footerCount!==1)failures.push(`smoke ${smoke.pathname}: status=${smoke.status}, h1=${smoke.h1Count}, header=${smoke.headerCount}, footer=${smoke.footerCount}`);if(smoke.retailGuideCount!==0)failures.push(`smoke ${smoke.pathname}: retail renderer leaked`);}
await writeFile(path.join(reportDir,'visual-metrics.json'),`${JSON.stringify({status:failures.length?'FAILED':'PASSED',viewports:results,smokeRoutes:smokeResults,failures},null,2)}\n`);console.log(`COMMERCIAL RETAIL SHOP RENOVATION KL VISUAL ${failures.length?'FAILED':'PASSED'}`);results.forEach((item)=>console.log(`${item.name}: overflow=${item.overflow}, broken=${item.brokenImages}, console=${item.consoleErrors.length}, minFont=${item.minBodyFont}`));if(failures.length){failures.forEach((failure)=>console.error(`FAIL ${failure}`));process.exitCode=1;}
