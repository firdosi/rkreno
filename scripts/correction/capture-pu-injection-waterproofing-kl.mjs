import { createReadStream,existsSync } from 'node:fs';import { mkdir,writeFile } from 'node:fs/promises';import { createServer } from 'node:http';import path from 'node:path';import { chromium } from 'playwright';
const root=process.cwd(),dist=path.join(root,'dist'),reportDir=path.join(root,'reports/public/page-recovery/pu-injection-waterproofing-kl');await mkdir(reportDir,{recursive:true});const mime={'.html':'text/html; charset=utf-8','.css':'text/css','.js':'text/javascript','.png':'image/png','.jpg':'image/jpeg','.jpeg':'image/jpeg','.webp':'image/webp','.svg':'image/svg+xml'};const server=createServer((request,response)=>{const url=new URL(request.url,'http://127.0.0.1'),pathname=decodeURIComponent(url.pathname).replace(/^\/rkreno(?=\/|$)/,'')||'/';let file=path.join(dist,pathname.replace(/^\//,''));if(pathname.endsWith('/'))file=path.join(file,'index.html');if(!existsSync(file))return response.writeHead(404).end('Not found');response.setHeader('content-type',mime[path.extname(file).toLowerCase()]||'application/octet-stream');createReadStream(file).pipe(response);});await new Promise((resolve)=>server.listen(0,'127.0.0.1',resolve));const port=server.address().port,browser=await chromium.launch({headless:true}),route='/pu-injection-waterproofing-kl-how-to-fix-wall-cracks-permanently/',viewports=[{name:'desktop',width:1440,height:1000},{name:'tablet',width:768,height:1024},{name:'mobile',width:390,height:844}],results=[],smokeResults=[];
try{for(const viewport of viewports){const page=await browser.newPage({viewport}),consoleErrors=[],failedRequests=[];page.on('console',(message)=>{if(message.type()==='error')consoleErrors.push(message.text());});page.on('requestfailed',(request)=>failedRequests.push(request.url()));await page.goto(`http://127.0.0.1:${port}/rkreno${route}`,{waitUntil:'networkidle'});await page.evaluate(()=>{document.querySelectorAll('.pu-page img').forEach((image)=>{image.loading='eager';});window.scrollTo(0,document.documentElement.scrollHeight);});await page.waitForFunction(()=>[...document.querySelectorAll('.pu-page img')].every((image)=>image.complete&&image.naturalWidth>0),null,{timeout:10000});await page.evaluate(()=>window.scrollTo(0,0));await page.waitForTimeout(150);await page.evaluate(()=>document.querySelector('.rk-header')?.classList.remove('is-stuck'));await page.addStyleTag({content:'.rk-header.is-stuck .rk-header__desktop,.rk-header__desktop,.rk-header.is-stuck .rk-mobilebar,.rk-mobilebar{position:static!important;inset:auto!important}.skip-link:not(:focus){position:fixed!important;top:-1000px!important}'});const metrics=await page.evaluate(()=>({overflow:document.documentElement.scrollWidth-document.documentElement.clientWidth,brokenImages:[...document.querySelectorAll('.pu-page img')].filter((image)=>image.complete&&image.naturalWidth===0).length,h1Count:document.querySelectorAll('.pu-page h1').length,imageCount:document.querySelectorAll('.pu-page img').length,tocCount:document.querySelectorAll('.pu-toc a').length,causeCount:document.querySelectorAll('.pu-cause-grid article').length,processCount:document.querySelectorAll('.pu-process-list li').length,priceRowCount:document.querySelectorAll('.pu-price-table tbody tr').length,whatsappCount:document.querySelectorAll('.pu-page a[href^="https://wa.me/"]').length,faqCount:document.querySelectorAll('.pu-page details,.pu-page [data-faq]').length,minBodyFont:Math.min(...[...document.querySelectorAll('.pu-page p:not(.pu-kicker),.pu-page li,.pu-page td')].filter((node)=>node.getBoundingClientRect().height>0).map((node)=>parseFloat(getComputedStyle(node).fontSize)))}));if(viewport.name==='mobile'){const menuButton=page.getByRole('button',{name:/open navigation/i}).first();await menuButton.click();metrics.mobileMenuOpened=await page.locator('.rk-header.is-drawer-open').count()===1;await page.keyboard.press('Escape');}await page.screenshot({path:path.join(reportDir,`${viewport.name}.png`),fullPage:true});results.push({...viewport,consoleErrors,failedRequests,...metrics});await page.close();}
const smokePage=await browser.newPage({viewport:{width:390,height:844}}),routes=['/','/blog/','/waterproofing-contractor-kuala-lumpur/','/plaster-ceiling-contractor-kl/','/house-renovation-in-kuala-lumpur/','/aircond-installation-kl/','/upah-pasang-aircond-selangor-panduan-harga-pemasangan-2026/','/house-renovation-in-kuala-lumpur-the-ultimate-planning-cost-guide-2026/','/pakej-deep-cleaning-rumah-kl-termasuk-pre-hari-raya/','/commercial-retail-shop-renovation-in-kuala-lumpur/','/contact-us/'];for(const pathname of routes){const response=await smokePage.goto(`http://127.0.0.1:${port}/rkreno${pathname}`,{waitUntil:'networkidle'});smokeResults.push({pathname,status:response?.status()??0,h1Count:await smokePage.locator('h1').count(),headerCount:await smokePage.locator('.rk-header').count(),footerCount:await smokePage.locator('[data-shared-footer]').count(),puGuideCount:await smokePage.locator('[data-pu-injection-waterproofing-kl]').count()});}await smokePage.close();}finally{await browser.close();await new Promise((resolve)=>server.close(resolve));}
const failures=results.flatMap((item)=>[item.overflow>1?`${item.name}: overflow ${item.overflow}`:null,item.brokenImages?`${item.name}: broken images ${item.brokenImages}`:null,item.consoleErrors.length?`${item.name}: console errors`:null,item.failedRequests.length?`${item.name}: failed requests`:null,item.minBodyFont<16?`${item.name}: body text ${item.minBodyFont}px`:null,item.h1Count!==1?`${item.name}: H1 ${item.h1Count}`:null,item.imageCount!==1?`${item.name}: images ${item.imageCount}`:null,item.tocCount!==5?`${item.name}: TOC ${item.tocCount}`:null,item.causeCount!==3?`${item.name}: causes ${item.causeCount}`:null,item.processCount!==5?`${item.name}: process ${item.processCount}`:null,item.priceRowCount!==3?`${item.name}: prices ${item.priceRowCount}`:null,item.whatsappCount!==1?`${item.name}: WhatsApp ${item.whatsappCount}`:null,item.faqCount?`${item.name}: FAQ ${item.faqCount}`:null].filter(Boolean));if(!results[2].mobileMenuOpened)failures.push('mobile: navigation did not open');for(const smoke of smokeResults){if(smoke.status!==200||smoke.h1Count!==1||smoke.headerCount!==1||smoke.footerCount!==1)failures.push(`smoke ${smoke.pathname}: status=${smoke.status}, h1=${smoke.h1Count}, header=${smoke.headerCount}, footer=${smoke.footerCount}`);if(smoke.puGuideCount!==0)failures.push(`smoke ${smoke.pathname}: PU renderer leaked`);}await writeFile(path.join(reportDir,'visual-metrics.json'),`${JSON.stringify({status:failures.length?'FAILED':'PASSED',viewports:results,smokeRoutes:smokeResults,failures},null,2)}\n`);console.log(`PU INJECTION WATERPROOFING KL VISUAL ${failures.length?'FAILED':'PASSED'}`);results.forEach((item)=>console.log(`${item.name}: overflow=${item.overflow}, broken=${item.brokenImages}, console=${item.consoleErrors.length}, minFont=${item.minBodyFont}`));if(failures.length){failures.forEach((failure)=>console.error(`FAIL ${failure}`));process.exitCode=1;}
